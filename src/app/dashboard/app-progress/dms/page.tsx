import AppProgressPage from '@/components/app-progress-page';

export default function DmsPage() {
  return (
    <AppProgressPage
      title="DMS"
      description="Monitor development progress, milestones, and pending work for the Document Management System."
    />
  );
}
